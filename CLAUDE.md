# このリポジトリで作業するすべてのセッションへ

## 発信時刻（JST）は、必ず実際の時計から取得すること

Ken さんへの返信の末尾に「発信年月日時分秒（JST）」を書くときは、
**記憶や推測で数字を書いてはならない。** 毎回、返信の直前にコマンドを実行し、
その出力をそのまま使うこと。

```bash
TZ=Asia/Tokyo date +"%Y年%m月%d日 %H時%M分%S秒 JST"
```

これを実行せずに時刻を書いた場合、それは捏造であり、JST と表示すること自体が誤りになる。

### 経緯
2026年9月、実行せずに作った時刻を JST として表示し続けていたことが発覚。
原因は「会話内の指示は次のセッションに引き継がれない」こと。
この指示を会話ではなくこのファイルに置くのは、そのため。
